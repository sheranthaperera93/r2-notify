{
    "name": "r2-notify",
    "private": true,
    "workspaces": [
        "packages/*",
        "apps/*"
    ],
    "scripts": {
        "build:packages": "cd packages/r2-notify-client && npm run build && cd ../r2-notify-react && npm run build && cd ../../"
    },
    "devDependencies": {
        "@types/babel__core": "^7.20.5",
        "@types/babel__generator": "^7.27.0",
        "@types/babel__template": "^7.4.4",
        "@types/babel__traverse": "^7.28.0"
    }
}
