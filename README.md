# r2-notify-client

Framework-agnostic WebSocket notification client for **r2-notify-server**.  
Consumes real-time events and provides a simple API to emit notification actions.

## Install

```bash
npm i r2-notify-client
# or
yarn add r2-notify-client
# or
pnpm add r2-notify-client
```
